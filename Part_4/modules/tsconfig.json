{
  "compilerOptions": {
    "target": "ES6", // Компилируем в версию ES6,
    "module": "commonjs", // Метод подключения модулей (require, import, etc),

    "outDir": "./build", // Путь, куда будет сохранён скомпилированный bundle (проект)
    "rootDir": "./src", // Путь, где хранятся исходные файлы для сборки bundle (проекта)

    "baseUrl": "./", // Корень пути, для подключения модулей
    "paths": { // alias для подключения модулей. Без него, придётся подключать модуль с указанием пути до него
      "@lib": ["./lib/my-lib"], // "import { } from '@lib/some'"
    },
    "rootDirs": [], // Массив списка "baseUrl". Например, для объединения папок с модулями
    "typeRoots": [], // Массив списка путей, для поиска описаний. По default, они расположены в "node_modules/@types"
    "types": [], // Точный путь до типов

    "resolveJsonModule": true, // Авто-импорт конфигураций JSON. Крайне полезный для удобства параметр
  },
}